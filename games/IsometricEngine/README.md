Serve this folder to test locally.

From a terminal in IsometricEngine/web run:

```bash
python -m http.server 8000
```

Then open http://localhost:8000 in your browser. Click to set start (left) and goal (right), then press Space to compute path.
